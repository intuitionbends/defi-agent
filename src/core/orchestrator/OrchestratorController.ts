// here should be called from the routes (from front end)

// const [pools, sentiment, contracts] = await Promise.all([
//     mappingModule.getMatchingPools(preferences),
//     sentimentModule.getMarketSentiment(),
//     contractChecker.getAvailableImplementations()
//   ]);
  
//   const agentInput = {
//     preferences,
//     pools,
//     sentiment,
//     contracts
//   };
  
//   const result = await aiAgent.generate(agentInput);
  
//   res.json(result);

